@model IEnumerable<Models.ItemType>

@{
    ViewBag.Title = "ItemType";
}

<h2>ItemType</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id = 3 })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>     
        <td>
            @Html.ActionLink("Edit", "EditType", new {  id=item.typeID, content = 3 })        
        </td>
    </tr>
}

</table>
